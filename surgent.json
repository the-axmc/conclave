{
  "scripts": {
    "init": "bun install",
    "dev": "bun run dev",
    "lint": "bun run lint",
    "dev:convex": "convex codegen && tsc -p convex --noEmit && convex dev --once --typecheck=disable"
  },
  "name": "surgent-web-server"
}